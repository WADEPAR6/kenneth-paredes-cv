import React from 'react';
import PrincipalProps from '@/app/fonts/types/PrincipalProps';

function Contacts({ dictionary }: PrincipalProps) {
    return (
        <div className=''>{dictionary.contacts}</div>
        
    );
}

export default Contacts;